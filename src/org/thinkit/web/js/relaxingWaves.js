"use strict";

const CONTAINER_NAME = "relaxing-waves-container";

const createRelaxingWaves = () => {
  const containerObjects = document.getElementsByClassName(CONTAINER_NAME);

  containerObjects.array.forEach((container) => {});
};
